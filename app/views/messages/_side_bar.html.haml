.sidebar
  .sidebar-name
    .sidebar-name__top-item
      %p.sidebar-name__top-item__name
        = current_user.name
      .sidebar-name__top-item__icon
        .sidebar-name__top-item__icon__posts
          = link_to new_group_path  do
            = fa_icon 'edit'
        .sidebar-name__top-item__icon__config
          = link_to edit_user_path(current_user) do
            = fa_icon 'cog'
  .sidebar-talklist
    - current_user.groups.each do |group|
      .sidebar-talklist__contents
        = link_to '#' do
          .sidebar-talklist__contents__groupe
            = group.name
          .sidebar-talklist__contents__message
            メッセージはまだありません